# Cloud Functions Python Runtime requirements

# Flask for request handling (Cloud Functions provides this, but include for local testing)
flask>=2.3.0

# ChromaDB for vector storage
chromadb>=0.4.0

# LangChain for vector store integration
langchain-community>=0.0.20
langchain-core>=0.1.0
langchain-huggingface>=0.0.1

# Transformers and PyTorch for embedding models
transformers>=4.30.0
torch>=2.0.0
numpy>=1.24.0

# Sentence Transformers (may be needed for some models)
sentence-transformers>=2.2.0

# Standard library dependencies (usually included, but explicit is better)
# These are typically included in Python, but listing for completeness:
# json, re, pathlib, typing, os, sys - all standard library
